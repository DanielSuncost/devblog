Generate the scheduled DevBlog entry for this repo.

Repository: /abs/project
Host: hermes

Run these commands from the repository:
1. devblog track --repo /abs/project --once
2. devblog entry --repo /abs/project --host hermes
3. devblog publish --repo /abs/project --format public-md -o /abs/project/.devblog/public/latest.md
4. devblog status --repo /abs/project

Rules:
- Preserve `.devblog/state.json` idempotency; do not force duplicate windows.
- Use only git history and `.devblog/ledger.jsonl` evidence.
- If no new entry is generated, report the duplicate/no-change status.
